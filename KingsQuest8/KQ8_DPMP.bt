#include "KQ8_Common.bt"
// This template was generated by CODEX.
// DPMP palette map/cache resource with palette path and opaque payload.

LittleEndian();

typedef struct
{
    char magic[4];
    Assert(magic == "DPMP");
    uint palettePathLength;
    char palettePath[palettePathLength];
    if (FTell() < FileSize())
        ubyte payload[FileSize() - FTell()];
} KQ8_DPMP_File;

KQ8_DPMP_File file;
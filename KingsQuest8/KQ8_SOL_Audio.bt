#include "KQ8_Common.bt"
// This template was generated by CODEX.
// Sierra SOL audio resource used by KQ8 AUD and dialogue A* files.

LittleEndian();

typedef struct
{
    ubyte sciResourceType;
    ubyte headerSize;
    char magic[4];
    Assert(sciResourceType == 0x0D && headerSize == 0x0C && magic == "SOL\0");
    ushort sampleRate;
    ubyte format;
    uint dataSize;
    ubyte spare;
    ubyte encodedAudio[dataSize];
} KQ8_SOL_File;

KQ8_SOL_File file;
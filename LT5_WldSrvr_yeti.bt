struct LTString
{
	ushort length;
	char content[length];
};
struct LTVector
{
	float x, y, z;
};

BigEndian();

char magic[4]; // SDLW
int version; // 4
int numObjects;

struct ObjectDef {
    LTString type;
    LTString name;
    int idxObjectDb;
    LTVector pos;
    LTVector rot;
} Objects[numObjects] <optimize=false>;